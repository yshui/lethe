vertex ballv(
	vec2 position,
	vec2 translate,
	vec2 texture_coord,
	float angle
);
particle Ball {
	>() {
		render << `ballv(vec2(0, 0), vec2(0,0), vec2(0,0), 0),
			  `ballv(vec2(1, 0), vec2(1,0), vec2(1,0), 0),
			  0, 1;
	}
}

